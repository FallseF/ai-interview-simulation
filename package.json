{
  "name": "ai-interview-simulation",
  "version": "2.0.0",
  "type": "module",
  "description": "AI Interview Simulation - 2AI + 1 Human (Career Support) Demo",
  "private": true,
  "scripts": {
    "build": "npm run build:server && npm run build:frontend",
    "build:server": "cd server && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../frontend && npm install",
    "postinstall": "cd server && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^5.0.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.10.0",
    "@types/ws": "^8.5.10",
    "tsx": "^4.6.2",
    "typescript": "^5.3.2"
  }
}
